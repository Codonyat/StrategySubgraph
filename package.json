{
  "name": "strategy-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for indexing Strategy (MONSTR) contract events",
  "repository": "https://github.com/your-org/strategy-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ strategy-subgraph",
    "create-local": "graph create --node http://localhost:8020/ strategy-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ strategy-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 strategy-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.69.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
